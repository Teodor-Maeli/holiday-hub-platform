spring:
  mail:
    host: smtp.gmail.com
    password: <Login password to SMTP server>
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    username: <Login User to SMTP server>